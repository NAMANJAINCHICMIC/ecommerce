import { ReplaceNumberDirective } from './replace-number.directive';

describe('ReplaceNumberDirective', () => {
  
  it('should create an instance', () => {
    const directive = new ReplaceNumberDirective();
    expect(directive).toBeTruthy();
  });
});
